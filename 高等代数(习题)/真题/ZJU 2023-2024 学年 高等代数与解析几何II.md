一、（15分）化简曲面方程
$$
xy + yz + xz = a^2(a > 0)
$$
为标准方程，并指出它是什么曲面.

二、（12分）将 $f(x_1, x_2, x_3) = x_1^4 + x_2^4 + x_3^4$ 表示为初等对称多项式的多项式.

三、（15分）设 $V$ 是数域 $\mathbb{F}$ 上的线性空间， $W$ 是 $V$ 的子空间，
$$
\begin{aligned}
\pi : V &\to V / W\\
v &\mapsto \bar{v}
\end{aligned}
$$
是商映射（自然映射），$n \geq 1$.

（1）设 $v_1, \cdots, v_n \in V$, $\bar{v}_1, \cdots, \bar{v}_n$ 在 $V/W$ 中线性无关. 证明 $v_1, \cdots, v_n$ 线性无关.

（2）设 $v_1, \cdots, v_n \in V \backslash W = \{v \in V | v \notin W\}$.且 $v_1, \cdots, v_n$ 线性无关，问$\bar{v}_1, \cdots, \bar{v}_n$ 是否线性无关？说明理由.

四、（13分）设 $V$ 是数域 $\mathbb{F}$ 上的 $n$ 维线性空间，$V$ 上的线性变换 $\sigma$ 有互不相同的特征值 $\lambda_1, \cdots, \lambda_k$ 及相应的特征向量 $\alpha_1, \cdots, \alpha_k$.若有 $\alpha_1 + \cdots + \alpha_k \in W$ 且 $W$ 为 $\sigma$ 的不变子空间. 证明： $\dim W \geq k$.

五、（15分）求平面上的仿射变换 $f$ 的坐标表达式，使得 $f$ 把圆 $x^2 + y^2 = 1$ 变成圆 $(x - 1)^2 + (y + 1)^2 = 4$，且 $f$ 把 $x$ 轴变成 $y + 1 = 0$.

六、（15分）设 $n > 1$，$\alpha_1, \alpha_2, \cdots, \alpha_n$ 是数域 $\mathbb{F}$ 上的线性空间 $V$ 的一组基，$\lambda \in \mathbb{F}$. $f : V \to V$ 是 $V$ 上的线性变换，且 $f(\alpha_i) = \lambda \alpha_i + \alpha_{i - 1}, i = 2, 3, \cdots, n$; $f(\alpha_1) = \lambda \alpha_1$.

（1）写出 $f$ 在基 $\alpha_1, \cdots, \alpha_n$ 下的矩阵.

（2）若 $W$ 是 $V$ 的一个 $f$-不变子空间，$W \neq \{\theta\}$（零空间），$W \neq V$，问是否存在 $V$ 的一个 $f$-不变子空间 $U$，使得 $V = W \oplus U$？说明理由.

七、（15分）设 $A = \begin{pmatrix}
2 & 3 & 2\\
1 & 8 & 2\\
-2 & -14 & -3
\end{pmatrix}$.

（1）求 $A$ 在复数域上的 Jordan 标准形，

（2）求 $A^{20}$.