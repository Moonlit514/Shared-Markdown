本章要解决的问题：
(1)什么是定积分？
(2)怎么判定一个函数在某一区间内是可积的？
(3)定积分怎么计算？
### Most important
> [!NOTE] Part 1. 定积分的定义
> 设$f(x)$是定义于$[a,b]$上的**有界**函数，在$[a,b]$上任意取分点$\{x_{i}\}^{n}_{i=0}$，作成一种划分
>$$
>P:a=x_{0}<x_{1}<x_{2}<\dots<x_{n}=b
>$$
>并任意取点$\xi_{i}\in[x_{i-1},x_{i}]$，记$\Delta x_{i}=x_{i}-x_{i-1}$，令$\rVert T \rVert=\mathop{\max}\limits_{1\leq i\leq n}(\Delta x_{i})$
>若极限
>$$
>I=\lim_{ \rVert T \rVert \to 0} \sum_{i=1}^{n} f(\xi_{i})\Delta x_{i}
>$$
>存在，且极限值与**划分P**和**$\xi_{i}$取法**无关。则称$f(x)$在$[a,b]$上***$Riemann$可积***.
>
>用"$\epsilon-\delta$语言"表述
>设有定数$I$，$\forall \epsilon>0,\exists \delta>0,$使得对任意一种划分
>$$
>P:a=x_{0}<x_{1}<x_{2}<\dots<x_{n}=b
>$$
>和$\forall \xi_{i} \in [x_{i-1},x_{i}]$，只要$\rVert T \rVert=\mathop{\max}\limits_{1\leqslant i\leqslant n}(\Delta x_{i})<\delta$，便有
>$$
>\Big | \sum _ { i = 1 } ^ { n } f ( \xi _ { i } ) \, \Delta x _ { i } \; - \; I \, \Big | \; { < } \varepsilon
>$$
>记$$I=\int_{a}^{b}  \,f(x) \mathrm{d}x$$
>称$I$为$[a,b]$上的**定积分**.
>


思考：
1.由定义，哪些函数是容易看出不可积的？怎么判定？
2.$Riemann$积分与我们学过的极限，求和有什么不同？
3.由定义，可积的函数需要有哪些性质？不需要有哪些性质？

1.$Dirichelet$函数.
2.可以说$Riemann$积分所求的是一种“连续的和”，而积分不涉及求和，数列求和所求的是"离散的和".
3.需要的：**有界性**、**“足够少”的不连续点**.
   不需要的：**连续性**、可导性、单调性.

由此可以看出，可积是非常强的性质.
事实上，有时连**有界**都不需要，也能求出积分值。这就引出了[[反常积分]].
而且，闭区间上的单调函数、可导函数、连续函数都是必定可积的.


直接由定义判断函数是否可积要克服两个困难：一是划分P的任意性，二是$\xi_{i}$选取的任意性.
我们需要在区间$[x_{i-1}, x_{i}]$的$f(x)$上找一个新的变量能够绕过这两个困难.
(1)划分的困难：如果这个变量随着划分变细单调或不变，可以使问题简化.
(2)$\xi_{i}$选取的困难：找到$[x_{i-1},x_{i}]$的某个特征指标，从而忽略掉$\xi_{i}$的选取.
根据上述所需的变量，我们发现区间的最大值和最小值非常合适.
>[!INFO] $Darboux$和
>记
>$$
m _ { i } = \operatorname* { i n f } _ { x _ { i - 1 } \leqslant x \leqslant x _ { i } } f ( \, x \, ) \; , \; M _ { i } = \operatorname* { s u p } _ { x _ { i - 1 } \leqslant x \leqslant x _ { i } } f ( \, x \, )
>$$
>则
>$$\bar { S }  \equiv  \bar { S } ( \mathit { P } ) \equiv \; \sum _ { i  =  1 } ^ { n } M _ { i } \Delta x_{i}\quad 与\quad \underline { { { S } } } \equiv \underline { { { S } } } ( \ P )  \equiv  \sum _ { i \, = \, 1 } ^ { n } \, m _ { i } \Delta x _ { i }
>$$
>分别称为$Darboux$**大和**与$Darboux$**小和**.

>[!NOTE] Part 2. $Riemann$可积的充要条件:
>有界函数$f(x)$在$[a,b]$上$Riemann$可积充分必要条件
>(1)
>$$\operatorname * { l i m } _ { \rVert T \rVert \to 0 } \overline { { S } } \left(  P \right) = L = l = \operatorname * { l i m } _ { \rVert T \rVert \to 0  } \underline { { S } } \left(  P \right) $$
>(2)
>$$
>\operatorname* { l i m } _ { \rVert T \rVert \to 0 } \sum _ { i \mathop { = } 1 } ^ { n } \; \omega _ { i } \Delta x _ { i } = 0 
>\quad其中\omega _ { i }=M_{i}-m_{i}
>$$
>(3)$Lebesgue$判别法
>一个有界函数在闭区间$[a,b]$上黎曼可积，当且仅当它在该区间上**几乎处处连续** (continuous almost everywhere)，即其不连续点集是零测集.
>在一元积分下的表述：
>$\forall \epsilon>0 , \sigma>0$，存在划分P，使得$\omega_{i}\geqslant\epsilon$的区间$[x_{i-1},x_{i}]$的长度之和$$\sum_{w_{i}\geqslant\epsilon}<\sigma$$

思考：
1.一个可积函数，在某个可数集上改变它的值后，会影响其可积性吗？



根据定义以及判断可积性的充要条件
可以得到一些定积分的基本性质：线性性、乘积可积性、保序性、绝对可积性、区间可加性，以及**积分第一中值定理**
>[!NOTE] Part 3. 积分第一中值定理 
>设$f(x)$和 $g(x)$都在$[a,b]$上可积$,g(x)$在$[a,b]$上**不变号**，则存在 $\eta\in\left[\begin{array}{c}m,M\end{array}\right]$,使得
$$\int_a^bf(x)g(x)\mathrm{d}x=\eta\int_a^bg(x)\mathrm{d}x,$$
>这里$M$和$m$分别表示 $f(x)$在$[a,b]$的上确界和下确界。
特别地，若$f(x)$在$[a,b]$上**连续**，则存在$\xi\in[a,b]$,使得
$$\int_{a}^{b}f(x)g(x)\mathrm{d}x=f(\xi)\int_{a}^{b}g(x)\mathrm{d}x.$$
>更特别地，若$g(x)\equiv 1$，则有
>$$
>\int_{a}^{b}f(x)\mathrm{d}x=f(\xi)(b-a)
>$$
>
>我们可以有结论更强的：$\xi$可以在开区间$(a,b)$中取到，这是[[介值定理]]的强结论推广而来的.

证明思路：设$g(x)\geqslant 0$ ，$f(x)g(x)$有界，得到$\int_{a}^bf(x)g(x)\mathrm{d}x$有界. 立得初始形式。而后若$f(x)$在$[a,b]$上**连续**，则由介值定理立得。


介绍一个划时代的公式
>[!note] Part 4. $Newton-Leibniz$公式
>设$f(x)$在$[a,b]$上**连续**，$F(x)$是$f(x)$在$[a,b]$上的原函数，则成立
$$\int_{a}^{b} f(x)\mathrm{d}x = F(x)|_{a}^{b}$$
其中, $F(x)|_{a}^{b} = F|_{x=a}^{x=b} = F(b) - F(a)$

课本利用$Leibniz$积分法则证明。我们换一种思路：
>[!PROOF] $Newton-Leibniz$公式
>所用的定理或性质:
>(1)一致连续
>(2)微分第一中值定理
>
>证：
>
>$\begin{aligned}|\sum_{i=1}^{n}f(\xi_{i})\Delta x_{i}-(F(a)-F(b))&|=|\sum_{i=1}^{n}f(\xi_{i})\Delta x_{i}-(F(x_{0})-F(x_{n})|\\&=|\sum_{i=1}^{n}f(\xi_{i})\Delta x_{i}-(F(x_{0})-F(x_{1})-(F(x_{1})-F(x_{2}))-\cdots-(F(x_{n-1})-F(x_{n}))|\\&=|\sum_{i=1}^{n}f(\xi_{i})\Delta x_{i}-\sum_{i=1}^{n}f(x_{i-1})-f(x_{i})|\\&=|\sum_{i=1}^{n}\Delta x_{i}(f(\xi_{i})-\frac{F(x_{i-1})-F(x_{i})}{\Delta x_{i}})|\\&=|\sum_{i=1}^{n}\Delta x_{i}\left(f(\xi_{i})-f(\zeta_{i})\right)|\end{aligned}$
>因为$f(x)$在$[a,b]$上连续，所以$f(x)$在$[a,b]$上**一致连续**.
>$\forall$$\varepsilon>0,\exists\delta>0,\forall |\xi_{i}-\zeta_{i}|<\delta,$令$\Delta x_{i}<\delta$,则有$$|f(\xi_{i})-f(\zeta_{i})|<\frac{\varepsilon}{(b-a)}$$
>所以得到：
>$$\begin{aligned}\left|\sum_{i=1}^n\Delta x_i\left(f(\xi_i)-f(\zeta_i)\right)\right|&\leq\sum_{i=1}^n\Delta x_i\left|f(\xi_i)-f(\zeta_i)\right|\\&<\sum_{i=1}^n\Delta x_i\cdot\frac{\varepsilon}{(b-a)}\\&=(b-a)\cdot\frac{\varepsilon}{(b-a)}=\varepsilon\end{aligned}$$
>
>从而
>$$\lim_{ ||T|| \to 0 } \sum_{i=1}^{n} f(\xi_{i})\Delta x_{i}=\int_{a}^{b}f(x)\mathrm{d}x =F(a)-F(b)$$

思考：
1.若$f(x)$在$[a,b]$上**可积**，而非连续，能得出相同的结论吗？
2.为什么要求存在$F(x)$是$f(x)$在$[a,b]$上的原函数？存在没有原函数的可积函数吗？
3.在闭区间上存在有原函数但不可积的函数吗？如果原函数**有界**呢？

定积分的应用在此不再赘述